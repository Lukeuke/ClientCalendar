import React, {useEffect} from 'react';

function Calendars() {
  
  useEffect(() => {
    
      let token = window.localStorage.getItem("jwt");
      
  });
  
  return (
      <div></div>
  );
}

export default Calendars;